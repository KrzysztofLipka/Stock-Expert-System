import { } from '../store/models/Prices'



/*export const PricesToChartDataConverter = (company: Company) => {
    let res: any[] = []
    //var t = company.quotes.map(quote => {name: company.ticker})
    company.quotes.forEach(quote => {
        res.push({ name: quote.date, pv: quote.price })
    });
    //return Object.fromEntries(company.quotes)


}*/